# CLAUDE.md

Claude Codeがこのリポジトリで作業する際にセッション開始時に自動的に読み込む設定ファイル。
GitHub Copilotは`.github/copilot-instructions.md`を自動的に読み込む。内容が二重管理でずれないよう、
共通ルールは複製せず以下でインポートする（`@`はClaude Codeのファイルインポート構文）。

## 共通プロジェクトガイドライン

@.github/copilot-instructions.md

## 実装エージェントとしての役割・ゲート

@.github/agents/which-one-implementation.agent.md
